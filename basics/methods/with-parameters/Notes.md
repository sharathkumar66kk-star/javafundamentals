## Methods with Parameters
Methods can receive input values called parameters.

## How it works
- Parameters are defined in method signature
- Values are passed when calling the method
- Method can return a result using return keyword

## Key points
- Parameter types must match argument types
- return type must match the returned value
